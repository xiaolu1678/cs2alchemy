---

【三、重大 UI 改动记录（续）】

1️⃣ 库存管理（最终形态）

* 筛选栏：横向统一布局（日期 / 名称 / 平台 / 磨损等级 / 状态）
* 所有输入框 + Select：
  → 同高度（h-11）
  → 同圆角（rounded-2xl）
* 编辑模式按钮：
  → 改为浅蓝胶囊（统一风格）
* 修复 BUG：
  → 编辑模式下“查看全部”不消失

✔ 表格优化（重点）

* 使用 table-fixed
* 金额列全部右对齐：
  → 参考价/成本
  → 售价
  → 利润
* 利润颜色：
  → 正：绿色
  → 负：红色
* 平台列：
  → 去掉椭圆 badge，改为纯文字

---

2️⃣ 材料录入（重要回退决策）

⚠ 用户强制要求：

* 布局回退（不要乱改结构）
* 只改 UI，不改逻辑

✔ 当前规则：

* 保持原纵向结构
* 统一所有椭圆输入框尺寸
* 不允许“越改越丑”

✔ 新增功能：
材料名称智能提示：

* 从数据库取最近5个不同材料
* 点击直接填入

---

3️⃣ 材料记录表

✔ 问题修复：

* 日期列 → 右移
* 平台列 → 去掉椭圆
* 金额列 → 完全右对齐（已多次修复）

✔ 核心代码原则：

* 不能只用 text-right
* 必须用：
  flex justify-end + table-fixed

---

4️⃣ 合成（合炉）模块

✔ 命名修改：

* 交易 → 合成
* 交易录入 → 合炉录入

✔ 布局：

* 上：录入
* 下：汰换记录

✔ ECO合炉 / 包炉：

* 切换 UI 重做
* 统一风格

✔ 最大问题（反复强调❗）：
👉 所有输入框必须一样大

包括：

* 汰换结果
* 开炉费比例
* 产物磨损等级
* 产物磨损区间
* 包炉售价
* InfoBox（参考价）

✔ 最终统一方案：

* 全部 h-11
* 全部 w-full
* 全部 rounded-2xl

---

5️⃣ 汰换记录

✔ 默认行为：

* 显示 20 条
* 可展开查看全部

✔ 搜索支持：

* 日期 / 类型 / 合同 / 产物 / 状态 / 结果

✔ 编辑模式：

* 类似库存管理
* 整体编辑（不是逐行展开）

✔ UI统一：

* 编辑按钮 → 浅蓝胶囊
* 保存 / 取消 → 同高度

---

6️⃣ 收益 / 日历模块

✔ 删除内容：

* 红绿底条（全部移除）
* 重复卡片（本日 / 本月 / 其他）

✔ 右侧结构：

* 当日净收益（大卡）
* 材料毛利
* 产物利润
* 开炉费
* 其他收入
* 过往收益

✔ 过往收益：

* 不再展开
* 默认隐藏（••••）
* 用 👁 控制显示

---

【四、公告系统（新增核心功能）】

📍 位置：
会员剩余 和 总收益 中间

✔ 功能：

* 未读 → 红点
* 点击 → 消失
* 更新版本 → 所有人重新红点

✔ 当前实现（前端）：
localStorage + version控制

const ANNOUNCEMENT_VERSION = "2026-05-04-ui-v4";

✔ 更新方法：
只需要改 version：

"2026-05-05-hotfix"
→ 所有人重新看到红点

✔ 内容支持：

* 多行（数组）
* 自动换行显示

---

【五、已踩坑总结（非常重要）】

❌ 常见报错：

1. JSX 未闭合
2. className 字符串断裂
3. grid-cols-[...] 被破坏
4. 三元表达式缺括号
5. Input 少引号

✔ 结论：
👉 不能大规模盲替换 JSX

---

【六、Git 状态】

✔ 已成功 push：

git push origin main

→ GitHub 已同步

---

【七、用户偏好（必须遵守）】

❗ 不要：

* 解释一堆
* 给 demo
* 给简化代码

❗ 必须：

* 直接给完整版本
* UI 一次改对
* 所有组件统一

用户原话核心：
👉 “越改越丑不要再发生”
👉 “必须全部一样大小”

---

【八、下次继续用这段】

直接复制：

继续日进斗金 v4.0 项目。不要给简化版，我要完整 page.tsx。当前要求是：不改逻辑，只继续统一 UI。重点处理所有输入框/选择框/InfoBox 同宽同高；库存和材料表格金额列完全右对齐；汰换记录编辑UI统一；公告已接入 version 红点机制。

---

# 【备份结束】
